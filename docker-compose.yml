services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: homelab
      POSTGRES_PASSWORD: homelab
      POSTGRES_DB: homelab
    ports:
      - '5432:5432'
    volumes:
      - homelab-db:/var/lib/postgresql/data

volumes:
  homelab-db:
